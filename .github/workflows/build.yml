name: Build

on:
  push:
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install SSHFS
        run: sudo apt install sshfs
      - name: Mount UE 5.4.4 install from a network drive
        run: sshfs ${{ secrets.SSHFS_USER }}@technicjelle@stack.technicjelle.com:/ue/Linux_Unreal_Engine_5.4.4/ ./ue/ -o reconnect -o password_stdin <<< '${{ secrets.SSHFS_USER }}'
      - name: Tree
        run: tree
